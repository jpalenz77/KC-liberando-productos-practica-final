coreDns:
  enabled: true
  service:
    selector:
      k8s-app: kube-dns

kubeControllerManager:
  enabled: false

kubeEtcd:
  enabled: true
  service:
    selector:
      k8s-app: ""
      component: etcd

kubeScheduler:
  enabled: false

kubeApiServer:
  enabled: true

kubelet:
  enabled: true

kubeDns:
  enabled: false

kubeProxy:
  enabled: true

kubeStateMetrics:
  enabled: true

nodeExporter:
  enabled: true
  jobLabel: node-exporter
  serviceMonitor:
    relabelings:
      - targetLabel: job
        replacement: node-exporter

prometheus-node-exporter:
  podLabels:
    jobLabel: node-exporter
  extraArgs:
    - --collector.filesystem.ignored-mount-points=^/(dev|proc|sys|var/lib/docker/.+|var/lib/kubelet/.+)($|/)
    - --collector.filesystem.ignored-fs-types=^(autofs|binfmt_misc|bpf|cgroup2?|configfs|debugfs|devpts|devtmpfs|fusectl|hugetlbfs|iso9660|mqueue|nsfs|overlay|proc|procfs|pstore|rpc_pipefs|securityfs|selinuxfs|squashfs|sysfs|tracefs)$

prometheus:
  enabled: true
  prometheusSpec:
    scrapeInterval: 30s
    scrapeTimeout: 10s
    enableAdminAPI: true
    externalUrl: ""
    ruleSelectorNilUsesHelmValues: true
    serviceMonitorSelectorNilUsesHelmValues: false
    podMonitorSelectorNilUsesHelmValues: false

defaultRules:
  create: true
  rules:
    alertmanager: true
    etcd: true
    configReloaders: true
    general: true
    k8s: true
    kubeApiserver: true
    kubeApiserverAvailability: true
    kubeApiserverSlos: true
    kubelet: true
    kubeProxy: true
    kubePrometheusGeneral: true
    kubePrometheusNodeRecording: true
    kubernetesApps: true
    kubernetesResources: true
    kubernetesStorage: true
    kubernetesSystem: true
    kubeScheduler: false
    kubeStateMetrics: true
    network: true
    node: true
    nodeExporterAlerting: true
    nodeExporterRecording: true
    prometheus: true
    prometheusOperator: true

## Configuration for alertmanager
alertmanager:
  config:
    global:
      resolve_timeout: 5m
    route:
      group_by: ['job']
      group_wait: 30s
      group_interval: 5m
      repeat_interval: 12h
      receiver: 'slack'
      routes:
      - match:
          alertname: Watchdog
        receiver: 'null'
    inhibit_rules:
      - target_match_re:
           alertname: '.+Overcommit'
        source_match:
           alertname: 'Watchdog'
        equal: ['prometheus']
    receivers:
    - name: 'null'
    - name: 'slack'
      slack_configs:
      - api_url: '' # <--- AÑADIR AQUÍ TU WEBHOOK DE SLACK
        send_resolved: true
        channel: '#tu-canal-slack' # <--- AÑADIR AQUÍ TU CANAL
        title: '[{{ .Status | toUpper }}{{ if eq .Status "firing" }}:{{ .Alerts.Firing | len }}{{ end }}] Simple Server Monitoring Alert'
        text: |-
          {{ range .Alerts }}
            *Alert:* {{ .Labels.alertname }} - `{{ .Labels.severity }}`
            *Description:* {{ .Annotations.message }}
            *Graph:* <{{ .GeneratorURL }}|:chart_with_upwards_trend:>
            *Details:*
            {{ range .Labels.SortedPairs }} • *{{ .Name }}:* `{{ .Value }}`
            {{ end }}
          {{ end }}

## Manages Prometheus and Alertmanager components
prometheusOperator:
  enabled: true

## Custom Prometheus Rules for Simple Server
additionalPrometheusRulesMap:
  simple-server-rules:
    groups:
      - name: simple_server_alerts
        interval: 30s
        rules:
          # Alert when Simple Server pod is down
          - alert: SimpleServerDown
            expr: up{job="simple-server"} == 0
            for: 1m
            labels:
              severity: critical
            annotations:
              summary: Simple Server is down
              description: "Simple Server instance {{ $labels.instance }} has been down for more than 1 minute."
              message: "Simple Server pod {{ $labels.pod }} is down"

          # Alert when container CPU usage exceeds limits
          - alert: SimpleServerCPUThrottlingHigh
            expr: |
              rate(container_cpu_cfs_throttled_periods_total{pod=~".*simple-server.*", container="simple-server"}[5m])
              / rate(container_cpu_cfs_periods_total{pod=~".*simple-server.*", container="simple-server"}[5m]) > 0.25
            for: 5m
            labels:
              severity: critical
            annotations:
              summary: Simple Server CPU throttling is high
              description: "Container {{ $labels.container }} in pod {{ $labels.pod }} is being throttled ({{ $value | humanizePercentage }} of the time)"
              message: "Simple Server container {{ $labels.container }} in pod {{ $labels.pod }} CPU throttling is above 25%"

          # Alert when container memory usage exceeds requests
          - alert: SimpleServerConsumingMoreThanRequest
            expr: |
              avg(container_memory_usage_bytes{pod=~".*simple-server.*", container="simple-server"}) by (pod) 
              > avg(kube_pod_container_resource_requests{resource="memory", pod=~".*simple-server.*", container="simple-server"}) by (pod)
            for: 2m
            labels:
              severity: critical
            annotations:
              summary: Simple Server consuming more memory than requested
              description: "Pod {{ $labels.pod }} is consuming more memory than requested"
              message: "Pod {{ $labels.pod }} memory usage is above the requested amount"

          # Alert when container CPU usage exceeds requests
          - alert: SimpleServerCPUConsumingMoreThanRequest
            expr: |
              avg(rate(container_cpu_usage_seconds_total{pod=~".*simple-server.*", container="simple-server"}[5m])) by (pod)
              > avg(kube_pod_container_resource_requests{resource="cpu", pod=~".*simple-server.*", container="simple-server"}) by (pod)
            for: 2m
            labels:
              severity: high
            annotations:
              summary: Simple Server CPU usage above requested
              description: "Pod {{ $labels.pod }} is consuming more CPU than requested"
              message: "Pod {{ $labels.pod }} CPU usage is above the requested amount"

          # Alert when container memory usage exceeds limits
          - alert: SimpleServerMemoryLimitReached
            expr: |
              avg(container_memory_usage_bytes{pod=~".*simple-server.*", container="simple-server"}) by (pod)
              > avg(kube_pod_container_resource_limits{resource="memory", pod=~".*simple-server.*", container="simple-server"}) by (pod) * 0.9
            for: 1m
            labels:
              severity: critical
            annotations:
              summary: Simple Server memory limit almost reached
              description: "Pod {{ $labels.pod }} is using more than 90% of its memory limit"
              message: "Pod {{ $labels.pod }} memory usage is at {{ $value | humanizePercentage }} of limit"

          # Alert for high request rate
          - alert: SimpleServerHighRequestRate
            expr: rate(server_requests_total[5m]) > 100
            for: 5m
            labels:
              severity: high
            annotations:
              summary: Simple Server high request rate detected
              description: "Simple Server is receiving more than 100 requests per second"
              message: "Request rate is {{ $value }} requests/second"

          # Alert when no requests received (possible issue)
          - alert: SimpleServerNoRequests
            expr: rate(server_requests_total[10m]) == 0
            for: 10m
            labels:
              severity: high
            annotations:
              summary: Simple Server not receiving requests
              description: "Simple Server hasn't received any requests in the last 10 minutes"
              message: "No requests received in the last 10 minutes - possible connectivity issue"

          # Alert for pod restart
          - alert: SimpleServerPodRestarting
            expr: rate(kube_pod_container_status_restarts_total{pod=~".*simple-server.*"}[15m]) > 0
            for: 5m
            labels:
              severity: high
            annotations:
              summary: Simple Server pod is restarting
              description: "Pod {{ $labels.pod }} has restarted {{ $value }} times in the last 15 minutes"
              message: "Pod {{ $labels.pod }} is restarting frequently"

grafana:
  plugins:
    - grafana-piechart-panel
  dashboardProviders:
    dashboardproviders.yaml:
      apiVersion: 1
      providers:
        - name: "default"
          orgId: 1
          folder: ""
          type: file
          disableDeletion: false
          editable: true
          options:
            path: /var/lib/grafana/dashboards/default
  dashboards:
    default:
      # Node Exporter dashboard
      node-exporter:
        gnetId: 11074
        revision: 9
        datasource: Prometheus
      # Node Exporter Full dashboard
      node-exporter-full:
        gnetId: 1860
        revision: 29
        datasource: Prometheus
      # Kubernetes cluster monitoring
      kubernetes-cluster:
        gnetId: 7249
        revision: 1
        datasource: Prometheus
      # Pod monitoring
      kubernetes-pods:
        gnetId: 6417
        revision: 1
        datasource: Prometheus